import { Route, Routes } from "react-router";

type Props = {
  isAllowed: boolean;
  token: string;
};

function AdminRoutes(props: Props) {
  return (
    <Routes>
      <Route></Route>
    </Routes>
  );
}

export default AdminRoutes;
