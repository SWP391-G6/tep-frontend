import { Route, Routes } from "react-router";

type Props = {
  isAllowed: boolean;
  token: string;
};

function MemberRoutes(props: Props) {
  return (
    <Routes>
      <Route></Route>
    </Routes>
  );
}

export default MemberRoutes;
